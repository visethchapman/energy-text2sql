# Slim runtime deps for the deployed demo (baseline + multi-agent, no RAG).
# Deliberately excludes torch / transformers / sentence-transformers / pgvector —
# those are only needed for the vector-RAG mode, which the hosted demo doesn't run.
psycopg[binary]>=3.2
httpx>=0.27
python-dotenv>=1.0
anthropic>=0.103.0
langgraph>=1.2.1
fastapi>=0.136.1
uvicorn[standard]>=0.47.0
slowapi>=0.1.9
